kind = "web"
previewPath = "/"
title = "روتيني - Kids Daily Routine"
version = "1.0.0"
id = "artifacts/kids-routine"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25666

[services.development]
run = "pnpm --filter @workspace/kids-routine run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/kids-routine", "run", "build" ]
publicDir = "artifacts/kids-routine/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25666"
BASE_PATH = "/"
